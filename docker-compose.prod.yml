version: '3'
services:
  elvis_api:
    env_file:
      - .env
      - .env.prod
  orientdb:
    volumes:
      - odb_data:/orientdb/databases
      - odb_backup:/orientdb/backup
volumes:
  odb_data:
  odb_backup:

